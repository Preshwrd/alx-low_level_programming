#include "main.h"
/**
*_abs- compute the absolute value of an integer
*@n: int to be converted to absolute
*Return: absolute value
*/
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (-n);
}
