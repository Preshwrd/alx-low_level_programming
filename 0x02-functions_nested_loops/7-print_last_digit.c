#include "main.h"
/**
*print_last_digit- to print thr last digit of a number
*
*@d: the last digit
*
*Return: last digit
*/
int print_last_digit(int d)
{
	int n;

	n = 0;
	if (d % 10 == n)

		return (n);
}
