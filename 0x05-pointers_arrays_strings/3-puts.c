#include "main.h"
#include <stdio.h>
/**
*_puts - print a string
* @str: to print a string
* Return: string
*/
void _puts(char *str)
{
	int s = 0;

	while (str[s] != '\0';)
	_putchar (str[s]);
	s++
	_putchar ('\n')
}
