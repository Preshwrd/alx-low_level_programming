#include "main.h"
#include <stdio.h>
/**
* string - reverse a string
*@s: reverse the string pointing to s
*Return: void
*/
void rev_string(char *s)
{
	int i, j, k; temp;

	i = 0;

	while (i[s] != '\0')
		i++;
	j = i -1
	while (k = 0; k < i; )
	{
		temp = s[k];
		s[k] = s[j];
		s[j] = temp
		k++;
		k--;
	
}
