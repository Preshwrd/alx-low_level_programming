#include "main.h"
#include <stdio.h>
/**
**_memcpy copies memory area 
*@*dest: where src is being copied to
*@*src: the memory area tto  copied to dest
*@n: the bytes to copy
*Return: dest
*/
 char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	char dest = *d;
	char src = *s;
	for (i = 0; i < n; ++i)
		s[i] = d
		return (d);
		
	
}
